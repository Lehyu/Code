package com.model;

import java.util.Comparator;

/**
 * Created by lhy on 10/23/15.
 */
public class BagComparetorByLabel implements Comparator{
    @Override
    public int compare(Object lhs, Object rhs) {
       return ((Bag<Integer>)lhs).getLabel() > ((Bag<Integer>)rhs).getLabel() ? 1:0;
    }
}
