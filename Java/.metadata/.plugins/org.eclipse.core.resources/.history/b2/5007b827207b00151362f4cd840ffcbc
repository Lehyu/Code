package com.entry;

import java.io.IOException;
import java.util.HashMap;

import com.Algorithm.Bayesian;
import com.tool.FileIOTool;

public class DMPro {
	private static final String filename = "/home/lhy/Code/Java/DMPro/data/data.txt";
	public static void main(String[] args){
		FileIOTool tool = new FileIOTool(filename);
		try {
			String[][] data = tool.read();
			Bayesian bay = new Bayesian(data, data[0].length-1,false);
			

	    	String str = "20|80";
	    	System.out.println(str.matches("\\|"));
	    	String[] tmp = str.split(">=|<=|!=|==|>|<");
	    	String value = tmp[1];
	    	tmp = str.split(value);
	    	String exp = tmp[0];
	    	System.out.println(exp);
	    	System.out.println(value);
	    	
		} catch (IOException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
	}
}
